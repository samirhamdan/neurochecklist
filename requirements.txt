# Um app so, um venv so. Reune o que o sistema de gestao e o gerador de logo
# precisam. O pacote morumbi3d (curadoria e geradores de letra) roda sem nada
# disto, mas aproveita numpy/scipy/trimesh quando estao presentes.
flask>=3.0
gunicorn>=21.2

# Gerador de logo 3D
numpy>=1.26
scipy>=1.11
opencv-python-headless>=4.8
pillow>=10.0
matplotlib>=3.7
shapely>=2.0
trimesh>=4.0
mapbox_earcut>=1.0

# Orcamento em PDF. Python puro (usa o pillow que ja esta acima para a
# marca), sem biblioteca do sistema -- pesa uns 3 MB, e num VPS de 800 MB
# isso importa. Uma alternativa como o weasyprint puxaria cairo e pango.
reportlab>=4.0

# Usados pelo trimesh ao contar corpos e ao cortar peca grande. Nao vem
# juntos e o erro aparece longe da causa.
networkx>=3.0
rtree>=1.0
